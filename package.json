{
  "name": "step-counter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "node scripts/dev-server.mjs",
    "build:widget": "node scripts/build-widget.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "vitest": "^3.2.4"
  }
}
